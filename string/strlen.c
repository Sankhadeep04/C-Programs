#include<stdio.h>
#include<string.h>
int main()
{
	int length;
	char ch[10];
	printf("enter to chech lenght : ");
	scanf("%s",&ch);

	length=strlen(ch);
	printf("the lenth of first string is %d",length);
}

