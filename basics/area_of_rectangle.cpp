#include<stdio.h>
int main()
{
	int l,w,a;
	printf("enter the length of rectangle - ");
	scanf("%d",&l);
	printf("enter the width - ");
	scanf("%d",&w);
	a=l*w;
	printf("the area of rectangle is %d",a);
}
